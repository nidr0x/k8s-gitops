apiVersion: v1
kind: Service
metadata:
  labels:
    app: homeassistant
  name: homeassistant
spec:
  ipFamilyPolicy: PreferDualStack
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 8123
      targetPort: http
  selector:
    app: homeassistant

