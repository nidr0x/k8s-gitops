apiVersion: v1
kind: Service
metadata:
  labels:
    app: snmp-exporter
  name: snmp-exporter
spec:
  type: ClusterIP
  ports:  
  - name: exporter
    port: 9116
    targetPort: 9116
    protocol: TCP
  selector:
    app: snmp-exporter

