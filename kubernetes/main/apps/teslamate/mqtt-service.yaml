apiVersion: v1
kind: Service
metadata:
  name: teslamate-mqtt
  labels:
    app: teslamate-mqtt
spec:
  type: ClusterIP
  ports:
    - name: mqtt
      protocol: TCP
      port: 1883
      targetPort: 1883
  selector:
    app: teslamate-mqtt
