apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: teslamate-mqtt
spec:
  refreshPolicy: CreatedOnce
  target:
    name: teslamate-mqtt
    creationPolicy: Owner
    template:
      type: Opaque
      mergePolicy: Merge
      data:
        username: teslamate
  dataFrom:
    - sourceRef:
        generatorRef:
          apiVersion: generators.external-secrets.io/v1alpha1
          kind: Password
          name: teslamate-mqtt-password
