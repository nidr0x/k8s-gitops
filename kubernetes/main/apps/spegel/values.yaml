spegel:
  containerdSock: /run/containerd/containerd.sock
  containerdRegistryConfigPath: /tmp/containerd/certs.d
service:
  registry:
    hostPort: 29999
serviceMonitor:
  enabled: false
grafanaDashboard:
  enabled: true
podLabels:
  app.kubernetes.io/component: spegel
