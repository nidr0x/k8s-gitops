apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-ingress
spec:
  podSelector: {}
  policyTypes:
    - Ingress
---
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: allow-from-homeassistant-proxies
spec:
  endpointSelector:
    matchLabels:
      app: homeassistant
  ingress:
    # Remote Companion traffic arrives directly from the Cloudflared pods.
    - fromEndpoints:
        - matchLabels:
            k8s:app: cloudflared
            k8s:io.kubernetes.pod.namespace: kube-system
      toPorts:
        - ports:
            - port: "8123"
              protocol: TCP
    # LAN traffic using either Gateway API listener is represented by Cilium's
    # reserved ingress identity, not by the host-networked Envoy pod identity.
    - fromEntities:
        - ingress
      toPorts:
        - ports:
            - port: "8123"
              protocol: TCP
---
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: allow-homeassistant-to-matter-server
spec:
  endpointSelector:
    matchLabels:
      app: matter-server
  ingress:
    - fromEndpoints:
        - matchLabels:
            k8s:app: homeassistant
            k8s:io.kubernetes.pod.namespace: homeassistant
      toPorts:
        - ports:
            - port: "5580"
              protocol: TCP
