apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: homeassistant-pdb
  namespace: homeassistant
spec:
  # HA is intentionally a singleton; allow node drains to evict it and restart it.
  maxUnavailable: 1
  selector:
    matchLabels:
      app: homeassistant
